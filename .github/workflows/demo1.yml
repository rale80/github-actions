name: Deploy App
on: 
  pull_request:
    types:
      - opened
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        uses: npm install
      - name: Run tests
        run: npm test
      - name: Build code
        run: npm run build
      - name: Deploy project
        run: echo "Deploying..."